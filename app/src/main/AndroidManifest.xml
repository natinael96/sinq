<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Only what reminders require — still NO network permission (PLAN.md §1.3). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Auto-granted; lets a prayer-alarm app fire exact alarms (setAlarmClock). -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Alarm-clock FGS type: apps holding USE_EXACT_ALARM are the documented
         eligible case for systemExempted (mediaPlayback was a policy mismatch). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.Agpeya">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".reminders.AlarmService"
            android:exported="false"
            android:foregroundServiceType="systemExempted" />

        <!-- Feeds the ግጻዌ widget's swipeable today/tomorrow stack. -->
        <service
            android:name=".widget.GitsaweWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <receiver
            android:name=".reminders.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminders.MarkDoneReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminders.StreakReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminders.GitsaweReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminders.SpecialHabitReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminders.BreathPrayerReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>

        <receiver
            android:name=".reminders.SystemEventsReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- የዕለቱ ግጻዌ home-screen widget. DATE_CHANGED rolls it over at midnight;
             the others cover reboots, manual clock changes, and app updates. -->
        <receiver
            android:name=".widget.GitsaweWidgetProvider"
            android:label="@string/widget_gitsawe_label"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_gitsawe_info" />
        </receiver>
    </application>

</manifest>
